language: node_js
node_js:
  - "10"
before_install:
  - npm i -g nyc
before_script:
  - psql -c "CREATE DATABASE games;" -U postgres
script:
  - npm run lint
  - npm run coveralls
notifications:
  email: false
